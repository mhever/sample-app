apiVersion: v1
kind: ConfigMap
metadata:
  name: sample-app-config
data:
  SERVICE_NAME: "sample-app"
  VERSION: "0.1.0"
  # Example Postgres DSN. Replace in your environment/secret for production.
  DB_DSN: "postgres://postgres:password@postgres:5432/postgres?sslmode=disable"
